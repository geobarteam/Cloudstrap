{
  "version": "0.2.0",
  "configurations": [
    {
      // F5: run the WASM Test Project — debugs the Bff (server) process and opens the browser.
      "name": "WASM Test Project (Bff server)",
      "type": "coreclr",
      "request": "launch",
      "preLaunchTask": "build-wasmtestproject",
      "program": "${workspaceFolder}/src/demo/BlazorWasm/Bff/bin/Debug/net10.0/Cloudstrap.WasmTestProject.Host.Bff.dll",
      "cwd": "${workspaceFolder}/src/demo/BlazorWasm/Bff",
      "env": {
        "ASPNETCORE_ENVIRONMENT": "Development",
        "ASPNETCORE_URLS": "https://localhost:7200;http://127.0.0.1:5300"
      },
      "serverReadyAction": {
        "action": "openExternally",
        "pattern": "\\bNow listening on:\\s+(https?://\\S+)"
      }
    },
    {
      // Debug the C# running INSIDE the browser (breakpoints in Presentation / Host.Wasm).
      // Launches the Bff as the host and attaches the WASM debugger to Chrome/Edge.
      "name": "WASM Test Project (client debug)",
      "type": "blazorwasm",
      "request": "launch",
      "hosted": true,
      "preLaunchTask": "build-wasmtestproject",
      "program": "${workspaceFolder}/src/demo/BlazorWasm/Bff/bin/Debug/net10.0/Cloudstrap.WasmTestProject.Host.Bff.dll",
      "cwd": "${workspaceFolder}/src/demo/BlazorWasm/Bff",
      "url": "https://localhost:7200",
      "env": {
        "ASPNETCORE_ENVIRONMENT": "Development",
        "ASPNETCORE_URLS": "https://localhost:7200;http://127.0.0.1:5300"
      }
    },
    {
      // The demo identity provider on 5310 — start it next to the Bff (or use the compound below)
      // so sign-in works outside the E2E fixture.
      "name": "WASM Test Project (IdP host)",
      "type": "coreclr",
      "request": "launch",
      "preLaunchTask": "build-wasmtestproject-idp",
      "program": "${workspaceFolder}/src/demo/Shared/IdentityProvider/bin/Debug/net10.0/Cloudstrap.WasmTestProject.Host.IdentityProvider.dll",
      "cwd": "${workspaceFolder}/src/demo/Shared/IdentityProvider",
      "env": {
        "ASPNETCORE_ENVIRONMENT": "Development",
        "ASPNETCORE_URLS": "http://127.0.0.1:5310"
      }
    }
  ],
  "compounds": [
    {
      // One F5 for the full demo: the identity provider plus the Bff server.
      "name": "WASM Test Project (full app + IdP)",
      "configurations": [
        "WASM Test Project (IdP host)",
        "WASM Test Project (Bff server)"
      ],
      "stopAll": true
    }
  ]
}
