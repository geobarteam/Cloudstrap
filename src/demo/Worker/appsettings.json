{
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    }
  },
  "Cloudstrap": {
    "Application": {
      "SystemName": "demo",
      "SubsystemName": "application",
      "SubsystemType": "worker",
      "EnvironmentTier": "Local"
    },
    "Logging": {
      "LevelOverrides": {
        "Microsoft.Hosting.Lifetime": "Information"
      }
    },
    "OpenTelemetry": {
      "Mode": "Console"
    },
    "Worker": {
      "HealthPort": 5350,
      "// HealthListenAddress": "localhost is the documented dev-time override (no firewall prompts on dev machines/CI agents); the shipped default '*' is the all-interfaces container posture.",
      "HealthListenAddress": "localhost"
    }
  },
  "Demo": {
    "// OutageSentinelPath": "Create this file to flip /ready to 503 (the outage drill); delete it to recover. The E2E suite overrides the path per run.",
    "OutageSentinelPath": "demo-outage.sentinel"
  }
}
